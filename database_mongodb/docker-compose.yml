version: '3'

services:
  database_mongodb:
    image: mongo:latest
    restart: always
    ports:
      - 29017:27017
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=dbpass123